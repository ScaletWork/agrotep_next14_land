/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { unoptimized: true },

  // For Build version

  // output: "export",
  // basePath: "/Agrotep",
  // assetPrefix: "/Agrotep/",
};

export default nextConfig;
