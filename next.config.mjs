/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { unoptimized: true },

  // output: "export",
  // basePath: "/Agrotep",
  // assetPrefix: "/Agrotep/",
};

export default nextConfig;
