/** @type {import('next').NextConfig} */
const nextConfig = {
  images: { unoptimized: true },

  // For hosting
  // output: "export",
  // trailingSlash: true,

  // For git
  // output: "export",
  // basePath: "/Agrotep",
  // assetPrefix: "/Agrotep/",
};

export default nextConfig;
